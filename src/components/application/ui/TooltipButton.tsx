import {Button, Tooltip} from "@mui/material";

export const TooltipButton = () => {
    return (
        <Tooltip title={"임시"}>
            <Button>
            </Button>
        </Tooltip>
    )
}